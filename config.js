// ── Backend API URL ──
// Change this ONE value whenever the backend URL changes (e.g. new Render
// deployment) and every page will pick it up automatically.
const API_BASE_URL = 'https://tech-o-nicks-program-backend.onrender.com';
